# Mijn App

Een coole web app gebouwd met Vite en TypeScript.

## 🚀 Gebruik

1. Download de code
2. Voer `npm install` uit
3. Voer `npm run dev` uit om te ontwikkelen
4. Voer `npm run build` uit voor productie

## 📁 Project Structuur

- `src/` - Broncode
- `package.json` - Dependencies en scripts
